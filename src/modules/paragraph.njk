<div class="paragraph-module module {{ classnames(customClasses) }}">
  <div class="paragraph-module__content">
    {% if isInline %}
      <p>{{ content | markdownInline | safe }}</p>
    {% else %}
      {{ content | markdown | safe }}
    {% endif %}
  </div>
</div>
